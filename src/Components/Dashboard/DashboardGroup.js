import React from 'react'

const DashboardGroup = ({ children }) => {
    return (
        <div className="boss-page-dashboard__group">
            {children}
        </div>
    )
}

export default DashboardGroup
