import React from 'react'

const ValueLine = ({ children }) => {
    return (
        <span className="boss-details__value-line">
            {children}
        </span>
    )
}

export default ValueLine
